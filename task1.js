function js_style() {
  // Selección por ID
  const paragraphEl = document.getElementById("text");

  // Cambia la letra, tamaño de letra y cambia a color morado
  paragraphEl.style.fontFamily = "Arial";
  paragraphEl.style.fontSize = "24px"; 
  paragraphEl.style.color = "#933becff";
}